const daysMonthCurrent = document.querySelector(".container-calendar");
const test = document.querySelector('#nextButton');
let divsDaysStart = [];
let divsDaysEnd = [];

/*
test.addEventListener('click', () => {
	for (let x = 0; x < 5; x++) {
		divsDaysStart.push(daysMonthCurrent.children.item(x));
	}

	for (let x = 37; x < 42; x++) {
		divsDaysEnd.push(daysMonthCurrent.children.item(x));
	}
})

*/
